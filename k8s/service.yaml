apiVersion: v1
kind: Service
metadata:
  name: trendify-service
  labels:
    app: trendify
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
  type: LoadBalancer
  selector:
    app: trendify
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000